#include <bits/stdc++.h>
#include <string>
using namespace std;
int main()
{   
    cout << "Koks jusu vardas" << endl;
    string vardas;
    cin >> vardas;
    string Sveikas;
    if(vardas.back()=='s')
        Sveikas = " Sveikas, ";
    else Sveikas = " Sveika, ";
    string bendras;
    bendras+="*";
    bendras+= Sveikas + vardas;
    bendras+= "! ";
    bendras+="*";
    int skc=0;
    skc = bendras.size();
    string pirma;
    string antra;
    for(int i=0; i < skc; i++)
        pirma+="*";
    for(int i=0; i <skc; i++)
    {
        if(i==0 || i == skc-1)
             antra+="*";
        else antra+= " ";
    }
    cout << pirma << endl << antra << endl << bendras << endl << antra << endl << pirma;
    return 0;

}
